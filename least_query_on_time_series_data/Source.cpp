#include <iostream>
#include <exception>
#include <string>

using namespace std;
/*
Given two timestamps, find the least query time to query the whole period between the two timestamps
you can only query whole year, month, day
Example:

Input: (20081001,20081101)
Output: 2
Explanation: query the whole month 200810, and then the day 20081101
Follow up:
can you do it considering the complement operation
for instance (20081001, 20081030) will output 2 because we can query the whole month 200810 and the day 20081031
and do a complement operation
*/
class Solution
{
public:

	int least_query_on_time_series(int from, int to) 
	{

	}
};

int main()
{
};
